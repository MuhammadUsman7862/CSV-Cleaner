# CSV-Cleaner
A lightweight Python script that reads a CSV with pandas, reports missing values, drops mostly-empty columns, fills gaps (mean/median/zero/drop), removes duplicates, and saves a cleaned copy — no data science background needed.
